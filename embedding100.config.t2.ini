# this file is used for extracting word2vec features

# the filename of word2vec
#word2vec.file = /home/zhaojiang/TE/Word2Vec/GoogleNews-vectors-negative300.bin
word2vec.file = /home/junfeng/Embedding/word2vec/word_vecs/embeddings-scaled.EMBEDDING_SIZE100.txt

# the format of word2vec (bin, plain)
word2vec.format = plain

# the dimension of word2vec
word2vec.dim = 100

# the way fitting word-level to sentence-level (split with ,, no whitespace)
# simple, idf, tfidf, other
word2vec.convey = idf

# the functions to extract feature
# consine,manhattan,euclidean,stats,diff
feature.funs = consine,manhattan,euclidean,stats

# the input file 
input.file = /home/junfeng/SemEval2016/T1/test-ascii/test/input.txt

# the delimiter of input file
input.delimiter = \t

# remove following elements
input.remove = stopwords,punctuations

# if lower()
input.lower = True

# the index of T, H, label, start with 0
indices = 0,1,2

# the output of feature file
output.feature.file = feature.w2v.embedding100.idf.lite


# the output of label file
output.label.file = label.txt

